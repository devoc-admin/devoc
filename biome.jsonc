{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  // 🤝 Assist actions
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on",
        "useSortedAttributes": "on",
        "useSortedKeys": "on",
        "useSortedProperties": "on"
      }
    }
  },
  // 💾 Biome uses Git to check which files are changed or staged
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true,
    "defaultBranch": "main"
  },
  "files": {
    "ignoreUnknown": true,
    "includes": [
      "!node_modules",
      "!.next",
      "!dist",
      "!build",
      // Remove preview apps
      "!**/preview",
      // 🧱 These components are pre-made components mostly copied/pasted from external registry so we don't want to apply to them linting rules
      "!**/components/ui",
      "!**/components/react-bits",
      "!**/components/fancy",
      "!**/components/aceternity",
      "!**/components/kibo-ui",
      "!**/components/sera-ui",
      "!**/components/magicui",
      "!**/components/tailgrids",
      "!**/components/motion-core",
      "!**/bilan",
      "!**/payload-types.ts",
      "!**/migrations",
      // 🖼️ SVG asset files are not source code — don't lint them as JSX
      "!**/*.svg"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "performance": {
        "noJsxPropsBind": "off" // Inline arrow functions in JSX props are our established style; React Compiler makes this micro-optimization obsolete
      },
      "style": {
        "useBlockStatements": "off", // No need to wrap every if statement in a block statement eg. if(!condition) return;
        "useConsistentTypeDefinitions": "off"
      },
      "suspicious": {
        // 🩹 Biome 2.5.7's type inference ignores explicit generic type arguments, so `useRef<HTMLDivElement>(null)`
        // is inferred as `RefObject<null>` and every `if (!ref.current) return;` guard is reported as redundant.
        // `useRef<T | null>(null)` is flagged too, so there is no code-level workaround. Remove once fixed upstream.
        "noUnnecessaryConditions": "off"
      }
    }
  },
  // 🌍 JavaScript globals for Chrome extension
  "javascript": {
    "globals": ["chrome"]
  },
  "extends": ["ultracite/biome/core", "ultracite/biome/next"]
}
